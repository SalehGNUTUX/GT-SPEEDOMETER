# سيناريوهات الاختبار

هذا الملفّ **شرطٌ من شروط ‎1.0.0‎**: «لا انهيارَ في سيناريوهات `TESTING.md`». وكان
المعيار يشير إلى ملفٍّ لم يُكتب، فكُتب هنا.

## كيف يُقرأ

- **آ** = آليّ، يمرّ في `./gradlew :app:testDebugUnitTest`.
- **ي** = يدويّ، لا سبيل إليه إلّا بجهاز.
- كلُّ سيناريو يذكر **ما يجب أن يقع**، لا «تحقّق أنّه يعمل».
- الأرقام تُملأ عند التشغيل. **فراغُ خانةٍ يعني أنّها لم تُجرَّب**، لا أنّها نجحت.

---

## ١. القياس — آ

مغطّاةٌ بـ‎37‎ اختبارًا في `app/src/test/`. تُشغَّل بأمرٍ واحد ولا تحتاج جهازًا:

| | الملفّ |
|---|---|
| المسافة والمدّة وزمن الحركة وأقصى سرعة ومرساة الفيديو | `TripRecorderTest` |
| بوّابة الدقّة والتنعيم وعتبة التوقّف | `SpeedFilterTest` |
| ملفّ GPX: بنيته وأرقامه وأزمنته وبياناته | `GpxWriterTest` |
| تنبيه السرعة ومدى القرص | `SpeedAlertTest` |
| فهرسة PMTiles | `PmtilesIndexTest` |

وقراءةُ أرشيفٍ حقيقيّ تحتاج ملفًّا لا يُودَع في المستودع:

```bash
GT_PMTILES=/مسار/morocco.pmtiles ./gradlew :app:testDebugUnitTest
```

---

## ٢. الرحلة والقياس — ي

| # | السيناريو | ما يجب أن يقع |
|---|---|---|
| ١.١ | ابدأ رحلةً داخل مبنًى بلا تثبيت | المدّة تدقّ من الثانية الأولى، والمسافة صفر، والأقمار تُعرض `0/n` |
| ١.٢ | امشِ ‎500‎ مترًا مقيسةً بأداةٍ أخرى | الفارق دون ‎%5‎ |
| ١.٣ | قف دقيقةً كاملة والرحلة جارية | المسافة لا تزيد، وزمن الحركة لا يزيد، والمدّة تزيد |
| ١.٤ | أوقِف مؤقّتًا ‎5‎ دقائق ثمّ استأنف | المدّة لم تعدّ الوقفة، والمسافة لم تقفز فجوتها |
| ١.٥ | ادخل نفقًا وأنت تقيس | لا قفزةَ مسافةٍ عند الخروج، والمعدّل المتحقّق يُظهر الانقطاع |
| ١.٦ | أنهِ الرحلة وافتحها من «الرحلات» | الأرقام كما كانت على الشاشة حرفيًّا |
| ١.٧ | افتح ملفّ GPX في تطبيقٍ آخر (OsmAnd · Strava) | يُقرأ بلا خطأ، والمسار في موضعه |

---

## ٣. الكاميرا والتسجيل — ي

| # | السيناريو | ما يجب أن يقع |
|---|---|---|
| ٢.١ | سجّل دقيقةً ثمّ أنهِ | الملفّ في `Movies/GT-SPEEDOMETER` ويُشغَّل، والطبقة محروقةٌ إن كان التوگل مشتغلًا |
| ٢.٢ | اضغط زرّ الشاشة الرئيسة والتسجيل جارٍ | التسجيل يستمرّ، والإشعار قائم |
| ٢.٣ | أطفئ الشاشة والتسجيل جارٍ | كذلك |
| ٢.٤ | بدّل العدسة والتسجيل جارٍ | يُغلق المقطع ويُفتح آخر، ولا ينقطع التصوير |
| ٢.٥ | **أدِر الهاتف والكاميرا مفتوحة (لا تسجّل)** | الواجهة تنتقل متحرّكةً، و**الصورة تستقيم معها** |
| ٢.٦ | **أدِر الهاتف والتسجيل جارٍ** | الملفّ يبقى على اتّجاهه الأوّل — لا نصفَ بميلٍ ونصفَ بآخر |
| ٢.٧ | سجّل عرضًا وشغّل الملفّ | الصورة عرضيّة، والطبقة في موضعها لا مقلوبة |
| ٢.٨ | التقط لقطةً والتسجيل جارٍ | لقطةٌ محروقة، والتسجيل لم ينقطع |
| ٢.٩ | اتّصالٌ وارد أثناء التسجيل | الملفّ يُغلق سليمًا أو يستمرّ — ولا يُفقد |
| ٢.١٠ | **قرصٌ يقارب الامتلاء والتصوير متّصل** | يُحذف أقدمُ تسجيل، ويُنتقل إلى مقاطع ‎3‎ دقائق، ورسالةٌ تمكث ‎5‎ ثوانٍ |
| ٢.١١ | **قرصٌ يقارب الامتلاء والتصوير مقسَّم** | يُحذف الأقدم ويمضي التصوير بلا تبديل |
| ٢.١٢ | ضع لقطةً وفيديو لك (لا من التطبيق) في المعرض ثمّ املأ القرص | **لا يُمسّان** — الحذف من مجلّد التطبيق وحده |

---

## ٤. الخرائط — ي

| # | السيناريو | ما يجب أن يقع |
|---|---|---|
| ٣.١ | افتح رحلةً بلا إنترنت وبلا أرشيف وبلا OsmAnd | مخطَّطُ مسارٍ يُرسم، ولا صندوقَ رماديّ |
| ٣.٢ | ضع ‎.pmtiles‎ لبلدك واختر «محلّيّة» | الخريطة تُرسم بأسمائها العربيّة موصولة |
| ٣.٣ | قرّب إلى أقصى حدّ | خطوطٌ حادّةٌ لا مربّعاتٌ ممطوطة (‎z18‎ من أرشيف ‎z14‎) |
| ٣.٤ | ضع أرشيفَي بلدين معًا | كلاهما يعمل بحسب موضع الرحلة |
| ٣.٥ | اعرض الخريطة بالإنترنت ثمّ اقطعه واختر «محلّيّة» | **لا تُعرض بلاطاتُ الشبكة المخبّأة** تحت شارة «محلّيّة» |
| ٣.٦ | اختر «OsmAnd» وOsmAnd مثبَّت | صورةٌ يرسمها OsmAnd، وحدُّ تقريبها معلومٌ (دَين مفتوح) |
| ٣.٧ | «فتح في OsmAnd» | يفتح OsmAnd مباشرةً بلا منتقٍ فيه محرّرُ مستندات |
| ٣.٨ | ضع ملفًّا خطأً (‎.osm.pbf‎ أو ‎.shp.zip‎) في مجلّد الخرائط | يُصنَّف «بيانات خام» ويُقال ذلك، ولا يُدَّعى أنّه خريطة |

---

## ٥. الواجهة والحالات — ي

| # | السيناريو | ما يجب أن يقع |
|---|---|---|
| ٤.١ | افتح «الرحلات» أوّل مرّةٍ بعد التثبيت | «يُقرأ سجلّ الرحلات…» ثمّ «لا رحلات محفوظة» — لا فراغٌ ولا خبرٌ كاذب |
| ٤.٢ | افتح «الرحلات» وعندك عشرون رحلة | حالةُ قراءةٍ ثمّ القائمة — ولا تظهر «لا رحلات» لحظةً واحدة |
| ٤.٣ | بدّل بين «الصور» و«التسجيلات» | المؤشّر الأخضر يستقرّ **تحت التبويب المختار** |
| ٤.٤ | افتح التطبيق ولديك إصدارٌ أحدث | شريطٌ أسفل الشاشة: «نزّل» · «لاحقًا» |
| ٤.٥ | اضغط «لاحقًا» ثمّ أعد فتح التطبيق | الشريط لا يعود **لهذا الإصدار**، ويعود لما بعده |
| ٤.٦ | أطفئ «أعلِمني بالتحديثات» | لا شريطَ ولا فحص |
| ٤.٧ | النافذة المصغَّرة (PiP) بالإيماءة وبالزرّ | تدخل في الحالين (دَين مفتوح: الشفافيّة) |
| ٤.٨ | اقسم الشاشة نصفين | التخطيط يتبع القيود لا اتّجاه الجهاز |

---

## ٦. البطّاريّة — ي · **الرقم مطلوبٌ لـ‎1.0.0‎**

المعيار: **نسبةٌ في الساعة تُنشر في `README`**. وهي غير مقيسةٍ بعد.

**الطريقة الموحَّدة** — تُعاد بها القياسات فتُقارَن:

1. اشحن إلى ‎100٪‎، وأطفئ الشاشة التلقائيّة، وثبّت السطوع على ‎50٪‎.
2. أطفئ الواي‑فاي وبيانات الجوّال (القياس لا يحتاجهما).
3. سجّل الحالة الابتدائيّة: النسبة، والوقت، ودرجة حرارة الجوّ.
4. شغّل السيناريو **ساعةً كاملة** دون لمس الجهاز.
5. سجّل النسبة النهائيّة.

| السيناريو | النسبة/ساعة | الجهاز | التاريخ |
|---|---|---|---|
| قياسٌ وحده (تبويب العدّاد، الشاشة مضاءة) | | | |
| قياسٌ والشاشة مطفأة | | | |
| تصويرٌ متّصل ‎1080p‎ بالطبقة المحروقة | | | |
| تصويرٌ مقسَّم ‎3‎ دقائق | | | |
| النافذة المصغَّرة فوق تطبيقٍ آخر | | | |

**ويُقارن بمرجع:** شغّل تطبيق كاميرا النظام ساعةً بالإعدادات نفسها. الرقم وحده لا
يعني شيئًا؛ الفارق عن المرجع هو ما يُقال للمستعمل.

---

## ٧. الأجهزة — **ثلاثةٌ شرطُ ‎1.0.0‎**

| الجهاز | أندرويد | ما جُرِّب | التاريخ |
|---|---|---|---|
| | | | |
| | | | |
| | | | |

وما جُرِّب على جهازٍ واحد **لا يُعَدّ مُثبَتًا**: أخطرُ ما في هذا التطبيق —
CameraX ودورةُ حياتها والخدمة الأماميّة وPiP — هو بعينه ما يختلف بين مُصنّعٍ وآخر.

---

## ٨. ما لا يُختبر هنا

- **`GpxReader`** يستعمل `android.util.Xml` فلا يعمل خارج أندرويد. موضعُه اختبارٌ
  على جهاز، أو تجربةُ فتح رحلةٍ قديمة (‎١.٦‎ أعلاه).
- **الراسم المتجهيّ** (`ShortbreadPainter`) يرسم بـ`android.graphics.Canvas`.
  يُفحص بالعين في ‎٣.٢‎ و‎٣.٣‎.
- **حدُّ امتلاء التخزين** يقتضي قرصًا يقارب الامتلاء فعلًا (‎٢.١٠‎–‎٢.١٢‎).
