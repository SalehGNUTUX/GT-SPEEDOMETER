# إسنادُ وحدة `osmand-api`

مجلّد `osmand-api/src/` **ليس من شفرة هذا المشروع**. هو نسخةٌ حرفيّة من وحدة
`OsmAnd-api` في مستودع OsmAnd:

- المصدر: <https://github.com/osmandapp/OsmAnd> ، المجلّد `OsmAnd-api/src`
- الحقوق: © OsmAnd BV ومساهمو OsmAnd
- الرخصة: **رخصة غنو العموميّة، الإصدار الثالث (GPLv3)** — وهي رخصة هذا المشروع
  نفسها، فالدمج مشروع ولا يفرض شرطًا زائدًا.

## ما هي

تعريفُ الواجهة الخارجيّة (`AIDL`) التي يعرضها تطبيق OsmAnd لغيره من التطبيقات:
‏101 ملفّ `.aidl` و‏102 ملفّ `.java` (أصنافُ `Parcelable` التي تعبر بين العمليّتين).
لا منطق فيها ولا خرائط — عقدُ اتّصالٍ فقط.

## لماذا نُسخت ولم تُجلب من مستودع تبعيّات

OsmAnd لا ينشر هذه الوحدة على Maven Central ولا على Google؛ ينشرها على مستودع
Ivy خاصّ به. وربطُ بناءِ هذا المشروع بمستودعٍ لا نملكه يجعل بناءه رهنَ بقاء ذلك
المستودع. والنسخ هو ما يفعله تطبيق OsmAnd النموذجيّ نفسه.

## ما الذي نستعمله منها

`IOsmAndAidlInterface.getBitmapForGpx` وحدَها عمليًّا: نمرّر إلى OsmAnd مسار
الرحلة بصيغة GPX فيعيد إلينا صورة الخريطة مرسومةً **من خرائطه المحلّيّة
المتّجهة**. وهذا هو الطريق الوحيد إلى خرائط ‎.obf‎ دون أنترنت: صيغةٌ متجهيّة
يرسمها محرّك OsmAnd، ولا يقدر osmdroid عليها، ولا سبيل إلى قراءة مجلّد OsmAnd
مباشرةً على أندرويد ‎11‎ فما فوق.

## تحديثها

OsmAnd يخلط `.aidl` و`.java` في شجرةٍ واحدة ويُعيد ضبط `sourceSets` ليقبلها
Gradle. وهنا فُصلتا إلى البنية المتعارَفة كي لا يعتمد البناء على سطر ضبطٍ يتبدّل
بين إصدارات AGP. فالتحديث فرزٌ لا نسخٌ مباشر:

```bash
git clone --depth 1 https://github.com/osmandapp/OsmAnd.git /tmp/osmand
cd /tmp/osmand/OsmAnd-api/src
rm -rf <المشروع>/osmand-api/src/main/{aidl,java}
find net -name '*.aidl' -exec install -D {} <المشروع>/osmand-api/src/main/aidl/{} \;
find net -name '*.java' -exec install -D {} <المشروع>/osmand-api/src/main/java/{} \;
```

لا تُعدَّل ملفّاتها في هذا المستودع أبدًا: أيّ تعديلٍ يضيع عند أوّل تحديث، وأسوأ
منه أنّه يُخالف عقد الواجهة فتنقطع المخاطبة بأخطاءٍ لا تدلّ على سببها.

## حذفها

هي تحسينٌ لا شرط. لإسقاطها: احذف المجلّد، واحذف سطر `include(":osmand-api")` من
`settings.gradle.kts` وسطر `implementation(project(":osmand-api"))` من
`app/build.gradle.kts`، ثمّ احذف `core/map/OsmAndBridge.kt`. يعود التطبيق حينها
إلى بلاطات الإنترنت وأرشيفات البلاطات المحلّيّة وحدها.
