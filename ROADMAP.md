<div align="center">

<img src="art/gt-speedometer-icon.png" width="120" alt="GT-SPEEDOMETER">

# خارطة الطريق إلى الإصدار المستقرّ الأوّل

**ما أُنجز · كيف نعمل · ما تبقّى**

`v0.9.6-beta` ← الحالة اليوم · `v1.0.0` ← الهدف

</div>

---

## ٠. لمن هذا الملفّ

هذا ملفُّ تسليمٍ يُقرأ **قبل** أيّ عملٍ على المشروع. غرضه أن يبدأ من يفتح
المستودع — أنتَ أو مساعدٌ برمجيّ — من حيث انتهينا لا من الصفر: يعرف ما بُني
ولماذا بُني هكذا، وما القرارات التي لا تُنقض، وما الذي بقي حتّى يصير الإصدار
مستقرًّا.

وهو **يكمّل `CLAUDE.md` ولا يُغنيه**. ذاك يحمل القواعد التي لا تُخالَف والمزالق
التقنيّة المكتشفة فعلًا؛ وهذا يحمل السياق والخطّة. اقرأ الاثنين.

ترتيب القراءة المقترح: `CLAUDE.md` ← هذا الملفّ ← `CHANGELOG.md` (للتفاصيل
الدقيقة لكلّ إصدار) ← الشيفرة.

---

## ١. المشروع في فقرة

**GT-SPEEDOMETER**: عدّاد سرعةٍ لأندرويد بـ Kotlin وJetpack Compose، ميزتُه التي
يقوم عليها **العدّاد فوق كاميرا حيّة مع تسجيل**. النمط المستهدَف أداةُ قياسٍ
رياضيّة، والتصميم للدرّاجة أوّلًا: مساحات لمسٍ كبيرة، وألوانٌ تُلمح قبل أن
تُقرأ، وصدقٌ في الأرقام.

- **الرخصة:** GPL-3.0
- **المستودع:** <https://github.com/SalehGNUTUX/GT-SPEEDOMETER>
- **الموقع:** `index.html` يُولَّد بـ `build.py` من `CHANGELOG.md`
- **الحجم اليوم:** ‎46‎ ملفّ Kotlin · نحو ‎20,000‎ سطر · ‎376‎ نصًّا
- **لا Google Play Services · لا Hilt · لا اعتماديّة مغلقة**

### الاعتماديّات كلّها

| المكتبة | الدور |
|---|---|
| AndroidX Core · Lifecycle · Activity Compose | الأساس |
| Compose BOM · Material 3 · Material Icons Extended | الواجهة |
| kotlinx-coroutines-android | التزامن |
| DataStore Preferences | التفضيلات |
| CameraX (core · camera2 · lifecycle · video · view · effects) | الكاميرا والحرق |
| osmdroid-android | الخرائط النقطيّة |
| `:osmand-api` (وحدة محلّيّة) | جسر AIDL إلى OsmAnd |

**قاعدة:** إضافة اعتماديّةٍ جديدة قرارٌ معماريّ لا تفصيل. لا تُضاف إلّا لسببٍ
مكتوبٍ في `CHANGELOG.md`.

---

## ٢. خريطة الشيفرة

```
app/src/main/java/net/gnutux/speedometer/
├── MainActivity.kt            النشاط الوحيد · PiP · السطوع · شفافيّة النافذة
├── SpeedoApp.kt               الحقن اليدويّ: SpeedoApp.engine
├── core/
│   ├── TripEngine.kt          ★ القلب: يجمع العيّنات ويوزّعها على الجميع
│   ├── DeviceTier.kt          الأجهزة المحدودة: isLowRamDevice ← liteMode
│   ├── alert/SpeedAlert.kt    ★ SpeedScale (المدى والعتبة والحدّ) + المنبّه ومشغّله
│   ├── location/              LocationEngine · SpeedFilter · SpeedSample · GnssInfo
│   ├── camera/                CameraSession · HudMetrics · VideoOverlayPainter
│   ├── map/                   OfflineMaps · OsmAndBridge · MapApps · MapDownloader
│   ├── media/MediaRepository  الصور والتسجيلات عبر MediaStore
│   ├── profile/VehicleProfile سيارة · دراجة · دراجة هوائية · مشي
│   ├── settings/AppSettings   ★ كلّ التفضيلات وكلّ التعدادات
│   ├── trip/                  TripRecorder · TripState · GpxWriter · GpxReader
│   └── update/UpdateChecker   الفحص والتنزيل والتثبيت من داخل التطبيق
├── service/                   TripService (خدمة أماميّة) · SpeedTileService
└── ui/
    ├── SpeedoViewModel.kt     الواجهة الوحيدة بين الشاشات والمحرّك
    ├── Format.kt              Fmt — كلّ رقمٍ معروض يمرّ من هنا
    ├── components/            SpeedGauge · GaugeStyles ★ · RouteMap · RouteSketch …
    ├── screens/               Speedometer · Digital · Camera · Media · Trips · Settings · Pip
    └── theme/Theme.kt         الألوان تُقرأ من التركيب لا من ثابت
```

### الملفّات الخمسة التي يجب أن تفهمها قبل أيّ تعديل

| الملفّ | لماذا |
|---|---|
| `core/TripEngine.kt` | نقطة الالتقاء الوحيدة: العيّنة تدخل مرّةً وتخرج إلى العدّاد والمسجّل والطبقة المحروقة والتنبيه. أيّ خللٍ هنا يظهر في أربعة أمكنة. |
| `core/alert/SpeedAlert.kt` | `SpeedScale` هو **الاشتقاق الواحد** للمدى والعتبة والحدّ. لا تحسب مدًى في شاشة. |
| `core/settings/AppSettings.kt` | كلّ تفضيلٍ وكلّ تعداد. الميزة الجديدة تبدأ عادةً بمفتاحٍ هنا. |
| `core/camera/CameraSession.kt` | `BindPlan` وسلسلة السقوط. أعقد ملفٍّ في المشروع. |
| `ui/components/GaugeStyles.kt` | هندسة التصاميم الستّة، يستهلكها ثلاثة مستدعين بألوانٍ مختلفة. |

---

## ٣. ما أُنجز: الطريق من ‎0.1.0‎ إلى ‎0.9.4‎

| الإصدار | جوهره |
|---|---|
| `0.1.0` | عدّادٌ يعمل: موقعٌ وسرعةٌ وقرص |
| `0.2.0` | ملفّات المركبات · تنعيمٌ لكلّ مركبة · حالة تموضعٍ صادقة |
| `0.3.0` | تسجيل الرحلات · GPX 1.1 صيغةً أصليّة · خريطة المسار |
| `0.4.0` | **الكاميرا**: عدّادٌ فوق البثّ الحيّ، وحرقُ الطبقة داخل الملفّ، وخدمةٌ أماميّة تُبقي التسجيل حيًّا |
| `0.5.0` | الإعدادات · السمة التلقائيّة بالساعة · مربّع الإعدادات السريعة · PiP |
| `0.6.0` | إصلاح تنسيق الطبقة المحروقة · متوسّط السرعة في الطبقة · فصل الصور عن التسجيلات · قسم «عن التطبيق» |
| `0.7.0` | **جسر OsmAnd** عبر AIDL · **حدّ السرعة** الذي يضبط القرص ويلوّنه · التنبيه الصوتيّ |
| `0.8.0` | OsmAnd يعمل وهو مغلق · تقريبٌ وتنقّل في صورته · مبدّل المصادر · إيقافٌ مؤقّت · وضع ليل/نهار · تحديدٌ متعدّد في الوسائط · دعم الأجهزة المحدودة |
| `0.9.0` | الكشّاف · تبديل العدسة والتسجيل جارٍ · **التصوير بالكاميرتين معًا** |
| `0.9.1` | مطويّات الإعدادات · اكتشاف تطبيقات الخرائط · ‎40‎ كم/س وحقلٌ يدويّ للحدّ |
| `0.9.2` | مُنزِّل أرشيفات الخرائط بحرّاسه الستّة · موقع المشروع · أتمتة البناء |
| `0.9.3` | تأكيد التسجيل · **التحديث من داخل التطبيق** · دعم ‎.gz‎ · رفض البلاطات المتجهيّة |
| `0.9.4` | **إصلاح التنبيه** (ثلاثة أعطال) · نغماتٌ وشدّة · ستّة تصاميم للعدّاد · النافذة المصغّرة · قائمة الخرائط بحذفٍ ومشاركة · قائمة المصادر المنسدلة |
| `0.9.5` | **إصدار نظافة**: CI حقيقيّ · أوّل اختبارات وحدة · جلب خريطةٍ من التخزين · تلوين حوار الخروج · تنظيف الجذر |
| `0.9.6` | **إصدار التصوير**: التسجيل عرضًا واتّجاهٌ مختار · **الطبقة تتبع التصميم** · حجم رقم السرعة · مشاركة التطبيق |

> التفاصيل الدقيقة — والأهمُّ منها **الأسباب** — في `CHANGELOG.md`. لا تكرّرها هنا،
> واقرأها قبل أن تمسّ ميزةً قديمة.

### القرارات المعماريّة التي لا تُنقض

1. **`elapsedRealtimeNanos` هو محور الزمن الوحيد.** `currentTimeMillis` تقفز مع
   مزامنة الشبكة، وقفزةٌ إلى الوراء تُسكت التنبيه دقائق.
2. **الفيديو نظيف والمسار منفصل.** الحرق خيارٌ لا وضعٌ وحيد.
3. **الحساب في المحرّك لا في الشاشة.** أيّ منطقٍ في `@Composable` يموت عند أوّل
   انطفاءٍ للشاشة — أي حين يحتاجه السائق.
4. **الاشتقاق الواحد.** المدى والعتبة والحدّ من `SpeedScale` وحده. أربع شاشاتٍ
   تحسبها بنفسها هي أربع فرصٍ لأن تختلف.
5. **الألوان تُقرأ من التركيب.** `DrawScope` ليس تركيبًا: تُرفع في جسم الدالّة
   ثمّ تُمرَّر، وإلّا انكسر الوضع الفاتح.
6. **لا تدّعِ ما لا تملك.** ملفٌّ لا يُرسم يُقال عنه ذلك صراحةً؛ ميزةٌ لا تعمل على
   كلّ جهازٍ يُقال إنّها كذلك. هذه القاعدة أنتجت: رفض `.obf`، ورفض MVT، ورفض
   بيانات Geofabrik الخام، وتحذير شفافيّة PiP.

---

## ٤. كيف نعمل — الطريقة التي أثبتت نفسها

هذه ليست شكليّات: كلّ حلقةٍ فيها وُضعت بعد عطبٍ وقع فعلًا.

### دورة الإصدار

```
١. تحديد النطاق   ← ما الذي يدخل هذا الإصدار، وما الذي يُؤجَّل ولماذا
٢. الفحص التقنيّ  ← هل المطلوب ممكن؟ قُل «لا» مبكّرًا لا متأخّرًا
٣. التنفيذ        ← ملفّاتٌ متباينة، لا يتقاطع عملان على ملفٍّ واحد
٤. التحقّق        ← الترجمة ← CLEANUP.sh ← اختبار المنطق الصافي
٥. التوثيق        ← CHANGELOG بالأسباب · README · تحديث الموقع
٦. التحزيم        ← مع مقارنة الأرشيف بالشجرة
٧. اختبارك أنت    ← لا رفع قبل إقرارك
٨. release.sh     ← الترقيم والتوقيع والوسم والرفع
```

### أدوات التحقّق في المستودع

> **حزمةٌ واحدة.** وقد كانت نكهتان (`lite` و`full`) أيّام تجربة MapLibre، فتعلّمتها
> الأدوات ثمّ بقيت فيها الحلقاتُ بلا كلفةٍ حين لا نكهة: `CLEANUP.sh` يمسح كلّ شجرةِ
> مصدرٍ موجودة، و`release.sh` **يكتشف** النكهات من `build.gradle.kts` ولا يكتب
> أسماءها. فإن عادت النكهات يومًا عملت الأدوات بلا تعديل.

| الأداة | ما تفحصه |
|---|---|
| `./gradlew :app:assembleDebug` | الترجمة الحقيقيّة. **الحكم النهائيّ.** |
| `.github/workflows/build.yml` | `CLEANUP.sh` ← اختبارات الوحدة ← `assembleDebug`، عند كلّ دفعةٍ وطلب دمج (أُنشئ في ‎0.9.5‎؛ كان هذا الجدول يُدرجه قبل وجوده) |
| `CLEANUP.sh` | ثمانية فحوص، انظر أدناه |
| `release.sh` | الترقيم · التوقيع · الوسم · الرفع · مزامنة الملفّات · حرّاس git |
| `build.py` | توليد `index.html` من `CHANGELOG.md` |

**فحوص `CLEANUP.sh` الثمانية:** تعريفاتٌ مكرّرة · مطابقة بيان الملفّات · موارد
النصوص المفقودة والمكرّرة · أسرار التوقيع المتتبَّعة · مخرجات البناء المتتبَّعة ·
نظافة الجذر · موارد لا يشير إليها شيء · **واجهات Material التجريبيّة بلا
`@OptIn`**.

> الفحص الثامن أُضيف بعد عطبٍ حقيقيّ: `SecondaryTabRow` كسر بناء ‎0.6.0‎ لأنّه
> واجهة تجريبيّة بلا إقرار، وفاحصُ الأنواع لم يرَه لأنّ R8 يجرّد `@RequiresOptIn`
> من الحزمة المستخرَجة. **درسٌ عامّ: الفاحص الذي لا يرى ليس فاحصًا.**

### قاعدة تُوفّر عليك ساعات

**كلّ `R.string.x` جديد يجب أن يوجد في `strings.xml` قبل استعماله.** أكثر ما
كسر البناء في هذا المشروع نصٌّ مستعمَلٌ بلا مقابل. `CLEANUP.sh` يفحص هذا (الفحص
الثالث) في ثانيةٍ واحدة — شغّله قبل Gradle لا بعده.

### كيف نستعمل الوكلاء المتوازين

في الإصدارات الكبيرة (‎0.9.4‎ مثلًا) قُسّم العمل على أربعة وكلاء متوازين. القاعدة
الوحيدة التي جعلت ذلك ينجح:

> **ملفّاتٌ متباينة تمامًا.** لا يلمس وكيلان ملفًّا واحدًا.

والملفّات المشتركة — `AppSettings.kt` و`strings.xml` و`SettingsScreen.kt` —
تُعدَّل **قبل** إطلاق الوكلاء: تُضاف المفاتيح والتعدادات والنصوص أوّلًا، ثمّ يقرؤها
الوكلاء ولا يكتبونها. ويُطلب من كلٍّ منهم في نهاية عمله أن يُرجع **توقيعات ما
أنشأ حرفيًّا**، ليستهلكها من يليه.

### قواعد المستودع

- كلّ commit وCHANGELOG وREADME **بالعربيّة الفصحى**.
- **باسم SalehGNUTUX وحده، بلا `Co-Authored-By`.**
- **لا رفع ولا نشر قبل اختبارك المحلّيّ وإقرارك.**
- `keystore.properties` و`*.jks` و`local.properties` **لا تُرفع أبدًا**. وضياع
  `gt-speedometer-release.jks` يعني فقدان القدرة على تحديث كلّ نسخةٍ منشورة إلى
  الأبد. **انسخه احتياطيًّا خارج الجهاز الآن إن لم تكن فعلت.**

---

## ٥. ديونٌ معروفة — مكتوبةٌ كي لا تُنسى

| # | الدَّين | الأثر | متى يُسدَّد |
|---|---|---|---|
| ٢ | شفافيّة النافذة المصغّرة غير مؤكَّدة على جهاز | قد تظهر صندوقًا أغمق لا شفّافًا | اختبارك |
| ٣ | إشارة إزاحة PiP في الكاميرتين معًا | قد تظهر المصغَّرة أسفل اليسار بدل ما هو مقصود | اختبارك |
| ٤ | قيمة `+1.5 EV` للوضع الليليّ | مقدَّرة لا مقيسة | اختبارك |
| ٥ | تخطيط SPLIT يضغط الصورتين رأسيًّا | لا اقتصاص في `LayoutSettings` | قيدُ CameraX |
| ٦ | `drawBarFace` لا يستردّ مساحة التدريج حين `ticks == null` | عولج عند المستهلك بـ`MiniBarFace` | ‎0.10.0‎ تنظيفًا |
| ٧ | **كلّ إصدارات المستودع مُعلَّمة `prerelease`** | من يُطفئ «النسخ التجريبيّة» لا يُعرض عليه شيء أبدًا | عند أوّل مستقرّ |
| ٨ | تغطية الاختبارات جزئيّة | ‎0.9.5‎ أدخل `SpeedAlert` و`SpeedScale` (‎11‎ اختبارًا). وبقيت `SpeedFilter` وGPX والمُنزِّل | ‎0.12.0‎ |
| ٩ | العربيّة وحدها، ولا وحدات ميل | يحصر التطبيق في نطاقٍ ضيّق | ‎0.11.0‎ |

---

## ٦. خارطة الطريق إلى ‎1.0.0‎

أربعة إصدارات. الترتيب مقصود: **الأصعب أوّلًا** بينما لا تزال التسمية تجريبيّة
تحتمل الخطأ، والتلميع أخيرًا.

---

### ‎0.10.0‎ — الخرائط · «خريطة بلدٍ كاملة دون اتّصال» — **أُنجز**

الهدف تحقّق، وبطريقٍ غير الذي خُطّط له.

**الخطّة كانت** إدخال MapLibre Native في نكهةٍ ثانية. **وما وقع** أنّ MapLibre جُرِّب
أسبوعًا فلم يرسم على جهاز الاختبار إلّا جزئيًّا، فحلَّ محلَّه محرّكُ رسمٍ كُتب لهذا
المشروع: يقرأ الملفّات نفسها (‎.pmtiles‎ بمخطّط shortbread) ويُخرجها بلاطاتٍ نقطيّةً
بلا مكتبةٍ أصليّةٍ واحدة، فبقيت الحزمة واحدةً ولم تكبر بميغابايتٍ واحد.

**التجربة موثَّقةٌ كاملةً في `docs/تجربة-الخرائط-المتجهية.md`** — المصادر التي فُحصت
واحدًا واحدًا، وما قِيس في أرشيفٍ حقيقيّ (كثافة المعالم بكلّ مستوى، وقيم `kind` كما
وردت)، والأعطاب التي كُشفت، والفرضيّة التي بقيت بلا فحص إن عاد أحدٌ إلى المتجهيّ.

**ما يعمل اليوم — أربعة مصادرَ للخريطة:**

| المصدر | ما هو |
|---|---|
| تلقائيّ | أحسن ما يتوفّر بالترتيب |
| إنترنت | بلاطاتٌ حيّة من الشبكة |
| OsmAnd | صورةٌ يرسمها OsmAnd من خرائطه، دون اتّصال |
| محلّيّة | أرشيف مجلّد الخرائط: ‎.mbtiles‎ و‎.sqlitedb‎ و‎.gemf‎ وأشجار البلاط، **و‎.pmtiles‎ المتجهيّة يرسمها محرّكنا** |

**وما بقي من ديون هذا القسم:**

- الرموز (مسجد · وقود · مدرسة) لا تُرسم: تحتاج مجموعة أيقوناتٍ كاملة.
- الاسم على حدّ البلاطة يُقصّ أو يتكرّر: يحتاج فهرسًا عامًّا للأسماء عبر البلاطات.
- الراسم بلا اختبارٍ آليّ: لا يعمل خارج أندرويد، فيُفحص على جهازٍ أو لا يُفحص.
- حدُّ تكبير OsmAnd بنيويّ: يرسم لنا صورةً واحدةً والتقريب بعدها رقميٌّ فيها. علاجُه
  إعادةُ طلب الصورة بحدودٍ أضيق كلّما قرّب المستعمل.

---

### ‎0.11.0‎ — الوحدات والتوطين · «ليست عربيّةً فقط، وليست كم/س فقط»

اليوم: العربيّة وحدها، وكم/س وحدها. وهذا يحصر أداةً جيّدة في نطاقٍ ضيّق بلا سبب
تقنيّ.

#### أ. وحدات السرعة والمسافة

مفتاحٌ في `AppSettings`: `SpeedUnit { KMH, MPH }`.

**ما يتأثّر — أحصِه كاملًا قبل أن تبدأ:**

| الموضع | التفصيل |
|---|---|
| `SpeedScale` | المدى والعتبة والحدّ كلُّها بالوحدة المختارة |
| `AppSettings.LIMIT_CHOICES` | حدود الميل غير حدود الكيلومتر: ‎20·25·30·40·55·65·70‎ |
| التصاميم الستّة | تدريج القرص |
| `HudMetrics` والطبقة المحروقة | **ملفّات الفيديو تحمل الوحدة، فالتبديل يغيّر ما يُحرق** |
| النافذة المصغّرة · التبويب الرقميّ · بلاطات الإحصاءات · سجلّ الرحلات | كلُّها |
| **GPX** | **لا يتأثّر.** المواصفة تخزّن م/ث دائمًا. لا تلمسه. |

**الفخّ:** رحلةٌ سُجّلت بالكيلومترات تُعرض لاحقًا بالأميال. القاعدة: **خزّن
دائمًا بالوحدات الأساسيّة (م/ث · متر)، وحوّل عند العرض فقط.** إن وجدت أيّ موضعٍ
يخزّن كم/س، فذاك عطبٌ يجب إصلاحه في هذا الإصدار.

#### ب. الإنجليزيّة

`values-en/strings.xml` بترجمة الـ‎376‎ نصًّا. ملاحظات:

- العربيّة تبقى `values/` أي **الافتراضيّة**، وهذا مقصود: التطبيق عربيٌّ أوّلًا.
- افحص كلّ تخطيطٍ في LTR — النصوص الإنجليزيّة أطول عادةً بنحو ‎30‎٪.
- `Fmt` يبقى على `Locale.US` للأرقام في اللغتين. **قاعدةٌ قائمة، لا تُنقض.**

#### ج. ~~سدُّ الدَّين الأوّل~~ — سُدِّد في ‎0.9.6‎

الحلّ لم يكن نقلًا ولا نسخًا: `CanvasDrawScope` يشغّل أوامر `DrawScope` على قماشٍ
أصليّ، فصارت `drawGaugeFace` نفسها ترسم الشاشة والفيديو. **ودرسٌ يُعمَّم: قبل أن
تنسخ هندسةً من واجهةٍ إلى أخرى، ابحث عن جسرٍ بينهما** — النسخة الثانية تتباعد عن
الأولى عند أوّل تعديل، والجسر لا يتباعد أبدًا.

ويبقى على هذا الإصدار أن يُمرّر الوحدة المختارة إلى الطبقة، وهو تغييرُ قيمةٍ لا هندسة.

#### معايير القبول

- [ ] التبديل إلى الميل يغيّر كلّ شاشة **وكلّ ما يُحرق في الفيديو**
- [ ] رحلاتٌ قديمة تُعرض صحيحةً بالوحدة الجديدة
- [ ] `.gpx` مطابقٌ بايتًا ببايت قبل التبديل وبعده
- [ ] الواجهة الإنجليزيّة كاملة بلا نصٍّ عربيٍّ متسرّب ولا تخطيطٍ مكسور
- [ ] التصميم المختار يظهر في الفيديو المحروق

---

### ‎0.12.0‎ — المتانة · «ما لا يُختبَر لا يُعتمد عليه»

كلُّ اختبارات هذا المشروع حتّى اليوم كانت **خارج المستودع ومؤقّتة**: مسارات سرعةٍ
محاكاة للتنبيه، ورسمٌ هندسيّ للتصاميم، وملفّات zip للحرّاس. كلُّها أثبتت جدواها
وكلُّها ضاعت. **أدخِلها المستودع.**

#### أ. اختبارات الوحدة — `app/src/test/`

| الوحدة | ما يُختبر |
|---|---|
| ~~`SpeedAlert`~~ | ✅ ‎0.9.5‎: العبور · التذبذب · التجاوز الممتدّ · الحكم على المعروض · `NaN` |
| ~~`SpeedScale`~~ | ✅ ‎0.9.5‎: اشتقاق المدى (`120 → 150`) · العتبة · مناطق الألوان |
| `SpeedFilter` | التنعيم · عتبة التوقّف · رفض الدقّة |
| `GpxWriter`/`GpxReader` | دورةٌ كاملة: اكتب ثمّ اقرأ فطابِق |
| `UpdateChecker.compareVersions` | ‎0.10.0 > 0.9.9‎ · اللواحق التجريبيّة |
| `MapDownloader` | صحّة الرابط · الامتداد · بصمة `.part` |
| `OfflineMaps.holdsTiles` | zip فيه بلاط مقابل zip أشكال ESRI |

بُدئ بـ`SpeedAlert` في ‎0.9.5‎ لأنّه أكثر ما انكسر ولأنّه دالّةٌ نقيّة. والباقي على المنوال نفسه.

#### ب. توسيع CI

المجرى قائمٌ منذ ‎0.9.5‎ ويشغّل `CLEANUP.sh` والاختبارات و`assembleDebug`. ما يبقى:
بوّابةُ تغطيةٍ دنيا، وتشغيلُ الاختبارات على أكثر من نسخة JDK إن دعت الحاجة.

#### ج. اختبارات الميدان — لا تُؤتمَت، وتُوثَّق

قائمةٌ تُنفَّذ يدويًّا قبل كلّ إصدار، تُكتب في `TESTING.md`:

1. تسجيلٌ متّصل ‎60‎ دقيقة: هل انقطع؟ هل سخن الجهاز؟ كم استهلك من البطّاريّة؟
2. امتلاء القرص أثناء التسجيل: هل حُفظ ما سُجّل؟
3. مكالمةٌ واردة أثناء التسجيل.
4. وصل سمّاعة بلوتوث وفصلها أثناء التنبيه — **الحالة التي كسرت ‎0.9.3‎**.
5. نفقٌ طويل: هل تجمّد العدّاد؟ هل عاد؟
6. جهاز Android Go: `liteMode` وسرعة أوّل تثبيت.
7. الوضعان الفاتح والداكن في كلّ شاشة.
8. أندرويد ‎9‎ و‎12‎ و‎14‎ و‎15‎: الأذونات والتخزين والخدمة الأماميّة.

#### معايير القبول

- [ ] تغطية اختبارات ‎core/‎ ≥ ‎70‎٪
- [ ] CI يمنع الدمج عند فشل اختبار
- [ ] `TESTING.md` منفَّذةٌ كاملةً وموثَّقة نتائجُها
- [ ] لا تسريب ذاكرةٍ في تسجيلٍ متّصل ساعةً

---

### ‎0.13.0‎ — التوزيع · «أن يجده الناس»

#### أ. F-Droid

المستودع الطبيعيّ لهذا التطبيق: GPL-3.0، بلا خدمات جوجل، بلا اعتماديّةٍ مغلقة.

المطلوب:
- بنية `fastlane/metadata/android/` بالوصف واللقطات وسجلّ التغييرات لكلّ لغة
- بناءٌ نظيفٌ من المصدر بلا خطوةٍ يدويّة
- مراجعة كلّ اعتماديّة: هل هي حرّة؟ `:osmand-api` — راجع `NOTICE-OsmAnd-API.md`
- وسمٌ في git لكلّ إصدار بصيغةٍ ثابتة (`release.sh` يفعل ذلك)

#### ب. سياسة الخصوصيّة

التطبيق **لا يُرسل شيئًا**. اكتب ذلك صريحًا في `PRIVACY.md` وعلى الموقع: لا
تحليلات، ولا حسابات، والبيانات كلُّها على الجهاز. وهذا في ذاته ميزةٌ تُذكر لا
تفصيلٌ قانونيّ.

#### ج. إنهاء وسم `prerelease`

**الدَّين السابع.** كلّ إصدارات المستودع اليوم `prerelease`، فمن يُطفئ «أشعِرني
بالنسخ التجريبيّة» لا يُعرض عليه شيءٌ أبدًا — لا خطأً ولا رسالة، صمتٌ تامّ. أوّل
إصدارٍ مستقرّ **يجب** أن يُنشر بلا هذه العلامة، وإلّا كانت ميزة التحديث كلُّها
ميّتةً عمليًّا.

#### د. الموقع

فعّل GitHub Pages من إعدادات المستودع. الموقع مبنيٌّ منذ ‎0.9.2‎ ولا يُخدَم.

---

### ‎1.0.0‎ — معايير الاستقرار

الإصدار المستقرّ ليس ميزةً جديدة، بل **وعدٌ**: أنّ ما فيه يعمل، وأنّ ما بعده لن
يكسره. لا تُطلقه قبل أن يتحقّق كلُّ ما يلي:

**الوظيفة**
- [ ] كلّ ميزةٍ معلَنة تعمل على ثلاثة أجهزةٍ حقيقيّةٍ مختلفة على الأقلّ
- [ ] لا ميزةً «تعمل أحيانًا» — إمّا تعمل أو تُعلَن حدودُها صراحةً
- [ ] الديون العشرة أعلاه: مسدَّدةٌ أو موثَّقةٌ بوعيٍ في `README.md`

**الجودة**
- [ ] لا انهيارٍ في سيناريوهات `TESTING.md`
- [ ] اختبارات الوحدة تمرّ، وCI أخضر
- [ ] استهلاك بطّاريّة مقيسٌ ومنشور (نسبة/ساعة تسجيل)
- [ ] `CLEANUP.sh` نظيفٌ تمامًا

**التوثيق**
- [ ] `README` يصف ما يفعله التطبيق **وما لا يفعله**
- [ ] `CHANGELOG` كامل من ‎0.1.0‎
- [ ] `PRIVACY.md` و`TESTING.md` موجودان
- [ ] الموقع يعمل ويشير إلى الإصدار المستقرّ

**التوزيع**
- [ ] إصدار GitHub **بلا `prerelease`**
- [ ] الحزمة موقَّعة، ومفتاح التوقيع منسوخٌ احتياطيًّا **خارج جهاز التطوير**
- [ ] F-Droid: مقدَّمٌ أو مبرَّرٌ تأجيله

**الوعد بعد ‎1.0.0‎**
- [ ] لا كسرَ لصيغة GPX
- [ ] لا فقدَ لتفضيلٍ عند التحديث
- [ ] لا رفعَ لـ`minSdk` في إصدارٍ ثانويّ

---

## ٧. أوّل جلسةٍ لك على Claude Code

```bash
git clone https://github.com/SalehGNUTUX/GT-SPEEDOMETER
cd GT-SPEEDOMETER

# اقرأ بهذا الترتيب
cat CLAUDE.md          # القواعد والمزالق
cat ROADMAP.md         # هذا الملفّ
head -110 CHANGELOG.md # آخر إصدارين

# تأكّد أنّ الشجرة سليمة قبل أن تمسّها
bash CLEANUP.sh
./gradlew :app:assembleDebug
```

### ما يجب أن تقوله للمساعد في أوّل رسالة

> اقرأ `CLAUDE.md` و`ROADMAP.md` قبل أيّ شيء. كلّ التعليقات والتوثيق بالعربيّة
> الفصحى بالأسلوب القائم في المشروع: تشرح **السبب** وتسمّي العطب الذي تتفاداه.
> لا اعتماديّة جديدة بلا قرارٍ صريح. شغّل `CLEANUP.sh` ثمّ `./gradlew
> :app:assembleDebug` قبل أن تقول «تمّ». لا ترفع شيئًا قبل أن أختبر وأُقرّ.

### مبادئ ثبتت في هذا المشروع

1. **قُل «لا» مبكّرًا.** ثلاث مرّات قلتُ «هذا لا يعمل» عن مصادر خرائط — وفي كلّ
   مرّة كان القول المبكر أرخص من ميزةٍ تُبنى ثمّ تُخرج شاشةً فارغة.
2. **الافتراضيّ يحفظ ما كان.** كلّ خيارٍ جديد قيمتُه الافتراضيّة هي السلوك
   القديم بحذافيره. من لم يختر شيئًا لا يتبدّل عليه شيء.
3. **علّق على السبب لا على الفعل.** `// يرسم القوس` عديم القيمة. `// الحكم من
   العقد المشترك لا من شرطٍ محلّيّ، وإلّا اختلف اللون بين شاشةٍ وملفّ` هو ما
   ينفع بعد ستّة أشهر.
4. **الفاحص الذي لا يرى ليس فاحصًا.** كلّما فاتت ثغرةٌ الفحصَ، أضِف فحصًا يمنع
   عودتها. هكذا صار `CLEANUP.sh` ثمانية فحوص.
5. **اختبر المنطق الصافي.** `SpeedAlert.onSample` دالّةٌ نقيّة، فمسار سرعةٍ
   محاكًى يكشف فيها ما لا تكشفه قيادةُ ساعة.

---

## ٨. المصادر

**المشروع**
- المستودع: <https://github.com/SalehGNUTUX/GT-SPEEDOMETER>
- مشاريع GNUTUX: <https://salehgnutux.github.io/gnutux/>

**الخرائط المتجهيّة — محرّكنا (‎0.10.0‎)**
- التجربة كاملةً: [`docs/تجربة-الخرائط-المتجهية.md`](docs/تجربة-الخرائط-المتجهية.md)
- **BBBike — أرشيفات ‎.pmtiles‎ لكلّ بلدٍ ومدينة** (وهو ما يُنزّله المستعمل): <https://data.bbbike.org/osm/region/>
- مواصفة PMTiles نسخة ‎3‎: <https://github.com/protomaps/PMTiles/blob/main/spec/v3/spec.md>
- مواصفة المربّعات المتجهيّة (MVT): <https://github.com/mapbox/vector-tile-spec/tree/master/2.1>
- مخطّط shortbread: <https://shortbread-tiles.org/schema/1.0/>
- منحنى هلبرت كما تستعمله PMTiles: <https://github.com/protomaps/PMTiles/blob/main/js/src/index.ts>

**ما جُرِّب ثمّ أُزيل — MapLibre Native**
- أمثلة PMTiles لأندرويد: <https://maplibre.org/maplibre-native/android/examples/data/PMTiles/>
- الاعتماديّة على Maven: <https://mvnrepository.com/artifact/org.maplibre.gl/android-sdk>
- Protomaps — الأنماط والبناء: <https://github.com/protomaps/basemaps>

**الخرائط النقطيّة (القائم)**
- osmdroid — أرشيفات دون اتّصال: <https://github.com/osmdroid/osmdroid/wiki/Offline-Map-Tiles>
- Mobile Atlas Creator: <https://mobac.sourceforge.io/>
- Maperitive — `generate-mbtiles`: <http://maperitive.net/docs/Commands/GenerateMBTiles.html>
- مثالٌ عمليّ لتوليد البلاطات: <https://afischer-online.de/and/aftrack/generate_offline_maps.html>
- Geofabrik — بيانات OSM: <https://download.geofabrik.de/>
- سياسة بلاطات OpenStreetMap: <https://operations.osmfoundation.org/policies/tiles/>

**المنصّة**
- CameraX: <https://developer.android.com/media/camera/camerax>
- «صورة في صورة»: <https://developer.android.com/develop/ui/views/picture-in-picture>
- DataStore: <https://developer.android.com/topic/libraries/architecture/datastore>
- OsmAnd — إنشاء خرائط دون اتّصال: <https://osmand.net/docs/technical/map-creation/create-offline-maps-yourself/>

**التوزيع**
- F-Droid — إدراج تطبيق: <https://f-droid.org/docs/Inclusion_Policy/>

---

<div align="center">

**الخلاصة في سطر**

المشروع على بعد أربعة إصدارات من الاستقرار: محرّكٌ متجهيّ، ثمّ وحداتٌ ولغة، ثمّ
اختباراتٌ ومتانة، ثمّ توزيع. وأصعبُها الأوّل، ولذلك هو الأوّل.

</div>
