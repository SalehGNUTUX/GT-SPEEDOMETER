# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# GT-SPEEDOMETER — قواعد العمل

عدّاد سرعة أندرويد بـ Kotlin + Compose، ميزته المميّزة: العدّاد فوق كاميرا حيّة مع تسجيل.
النمط المستهدَف: **أداة قياسٍ رياضيّة**، والتصميم **للدرّاجة أوّلًا**.

## قواعد لا تُخالَف

1. **`elapsedRealtimeNanos` هو محور الزمن الوحيد** في كلّ ما يخصّ القياس والمزامنة.
   `System.currentTimeMillis` ممنوع خارج كتابة زمن GPX المدنيّ.
2. **الفيديو نظيف، المسار منفصل.** أيّ ميزة جديدة تحترم هذا الفصل. الحرق داخل
   الفيديو توگل اختياريّ، لا الوضع الوحيد.
3. **لا Google Play Services** ولا أيّ تبعيّة تكسر التشغيل على أجهزة بلا خدمات جوجل.
4. **لا Hilt.** حقن يدويّ عبر `SpeedoApp.engine`.
5. **الأرقام المعروضة تُنسَّق بـ `Locale.US`** (انظر `ui/Fmt`): أرقام 0-9 لا ١-٩،
   ونقطة عشريّة لا فاصلة. ينطبق على الواجهة وعلى ملفّات GPX معًا — الفاصلة العشريّة
   تُفسد الملفّ لكلّ قارئ GPX.
6. **أسماء المركبات بالعربية كما اعتمدها المستخدم:** سيارة · دراجة (= الناريّة) ·
   دراجة هوائية · مشي. لا تُغيَّر ولا تُشكَّل.
7. **مساحات اللمس ≥ 56 نقطة، وأزرار الفعل الرئيسة ≥ 72:** تُضغط بقفّاز الدراجة.
8. **لا تدّعِ دقّةً لا نملكها.** المعدّل المتحقّق وعدد الأقمار يُعرضان دائمًا. وهذه
   القاعدة تمتدّ إلى **الموقع العامّ**: جدول التنزيل لا يعرض رابطًا لحزمةٍ غير موجودة.
9. **نكهتان للتوزيع، وميزاتُهما واحدةٌ حرفًا بحرف.** `libre` (جيت‌هاب · F-Droid)
   و`play` (المتجر). وجُرِّبت قبلهما نكهتان تختلفان في **المحرّك** (`lite`/`full`)
   في ‎0.10.0‎ فأُزيلتا (`docs/تجربة-الخرائط-المتجهية.md`)، والقاعدة باقيةٌ على
   حالها: **لا نكهةَ لفرقٍ في الميزات**. والفرق الوحيد المسموح فرقُ سياسةِ متجرٍ
   لا يُحتال عليه — وهو اليوم واحد: **بلاي يمنع أن يحدّث التطبيق نفسه**، فنكهته
   بلا `UpdateChecker` ولا `REQUEST_INSTALL_PACKAGES` ولا نصوصِهما.
   انظر `docs/الرفع-إلى-غوغل-بلاي.md`.

## قواعد المستودع

- كلّ commit و CHANGELOG و README و`docs/` بالعربيّة الفصحى.
- باسم SalehGNUTUX فقط، بلا `Co-Authored-By`.
- لا رفع ولا نشر قبل أن يختبر المستخدم محلّيًّا ويُقرّ.

## البناء والفحص

```bash
./gradlew :app:assembleLibreDebug     # الحكم النهائيّ على الترجمة
./gradlew :app:assemblePlayDebug      # ونكهة المتجر معها: الحاجز اسمٌ بجسدين
./gradlew :app:testLibreDebugUnitTest # اختبارات الوحدة (الشجرة مشتركة)
./gradlew :app:bundlePlayRelease      # رزمة `.aab` لبلاي
bash CLEANUP.sh                       # ثمانية فحوصٍ على الشجرة، في ثانية
```

**وبناءُ نكهةٍ واحدةٍ لا يكفي حكمًا:** `ui/update/UpdateSurface.kt` أربعةُ أسماءٍ
بجسدين، فمن عدّل أحدهما ولم يعدّل الآخر لا يكسر إلّا النكهة التي لم يبنِها.

**اختبارٌ واحد بعينه:**

```bash
./gradlew :app:testLibreDebugUnitTest --tests '*PmtilesIndexTest*'
```

**اختبار قراءة أرشيف PMTiles** يُتخطّى ما لم يوجد أرشيفٌ حقيقيّ (‎190‎ م.ب لا تُودَع
في المستودع). لتشغيله فعلًا:

```bash
GT_PMTILES=/مسار/morocco.pmtiles ./gradlew :app:testLibreDebugUnitTest
```

Gradle 8.11.1 · AGP 8.9.2 · Kotlin 2.1.0 · compileSdk 36 · minSdk 26 · osmdroid 6.1.20.

**و`targetSdk 36` شرطُ بلاي منذ ‎31‎ غشت ‎2026‎**، لا خيارٌ يُراجَع.

### حين تضيق ذاكرة جهاز البناء

`lintVitalAnalyze…Release` أثقلُ خطوةٍ في البناء، وتُقتل بلا رسالةٍ على جهازٍ بـ‎8‎
غ.ب مشغول. لحزم التجربة وحدها:

```bash
./gradlew :app:assembleLibreRelease --no-daemon --no-parallel --max-workers=1 \
  -x lintVitalAnalyzeLibreRelease -x lintVitalReportLibreRelease
```

**والاسم بالنكهة لا بدونها.** `-x lintVitalReportRelease` كان صالحًا قبل النكهتين،
وصار اليوم **ملتبسًا فيسقط البناء**: «Task is ambiguous … candidates are
`lintVitalReportLibreRelease`, `lintVitalReportPlayRelease`». وللرزمة:

```bash
./gradlew :app:bundlePlayRelease --no-daemon --no-parallel --max-workers=1 \
  -x lintVitalAnalyzePlayRelease -x lintVitalReportPlayRelease
```

ولا يُستثنى الفاحص في `release.sh`: الإصدار المنشور يمرّ به كاملًا. **فأخلِ الذاكرة
قبل أن تبدأه** — أُغلِق المتصفّح وما يشبهه — وإلّا قُتلت الخطوة بلا رسالة وسقط
الإصدار في منتصفه. (وقع في جلسة ‎1.0.0‎: قُتل البناء عند `lintVitalAnalyzeLibreRelease`
والمتاح ‎0‎ غ.ب.)

ونكهتان تعنيان خطوتَي فحصٍ لا واحدة، فالزمن يقارب الضِّعف.

### الإصدار

`./release.sh` يفعل كلّ شيء: رفعُ الرقم في `build.gradle.kts` و`README.md`، وتوليدُ
`index.html` من `CHANGELOG.md`، والبناء، والإيداع، والدفع **إلى `main` مباشرةً**
مهما كان الفرع، والوسم، وإنشاء إصدار GitHub ورفع الحزمة إليه.

**وحزمةُ `libre` وحدها تُرفع.** رزمةُ `play` تُبنى وتُنسخ إلى `dist/` **ولا
تُرفع**: `.aab` لا تُثبَّت بضغطةٍ على هاتف، وترفعها بيدك إلى Play Console.

```bash
./release.sh --dry-run                 # يعرض ما سيفعله بلا أن يفعل
./release.sh --code 18 --name 0.12.0-beta --type beta --variant release --yes
```

يشترط أن يحمل `CHANGELOG.md` قسمًا بصيغة `## v<الرقم>` — وإلّا سقط الإصدار من
الموقع صامتًا. والصيغة: `### أُضيف` / `### أُصلح` / `### أُزيل` ثمّ نقاطٌ
بـ`- **عنوان**`.

## أدوات التحقّق

| | |
|---|---|
| `CLEANUP.sh` | تعريفات مكرّرة · مطابقة بيان الملفّات · موارد النصوص · أسرار التوقيع · مخرجات البناء · جذر المشروع · موارد ميّتة · واجهات تجريبيّة |
| `.github/workflows/build.yml` | `CLEANUP.sh` ← اختبارات الوحدة ← `assembleDebug`، عند كلّ دفعة |
| `build.py` | يولّد `index.html` من `CHANGELOG.md`؛ يسأل GitHub عن مرفقات كلّ إصدار فلا يُبنى رابطٌ لحزمةٍ غير منشورة |

**`CLEANUP.sh` يحمل بيانًا صريحًا بأسماء ملفّات المصدر (`MANIFEST`).** أيُّ ملفٍّ
جديد لا يُضاف إليه يُعَدّ «مخلَّفًا» ويعرض `--fix` حذفه. أضِف اسمك إليه في الإيداع
نفسه الذي يُنشئ الملفّ.

## خريطة الشيفرة

```
core/
  TripEngine.kt        محرّك الرحلة: المسافة والسرعة والزمن
  location/            LocationEngine — مصدر الموقع الوحيد
  camera/              CameraSession (دورة حياة خاصّة) · VideoOverlayPainter (الحرق)
  map/                 OfflineMaps (القرار) · OsmAndBridge (جسر AIDL) · MapDownloader
    pmtiles/           PmtilesReader — ترويسة وأدلّة ومنحنى هلبرت
    mvt/               MvtTile — فاكُّ بروتوبَف بأقلّ ما يكفي
    render/            ShortbreadPainter — يرسم البلاطة على Canvas
  media/               MediaRepository (الحفظ) · StorageWarden (إدارة الامتلاء)
  settings/            AppSettings — DataStore، مصدر التفضيلات الوحيد
ui/
  screens/             CameraScreen · TripsScreen · SettingsScreen · MediaScreen · PipScreen
  components/          RouteMap · GaugeStyles · RouteSketch
```

**وشجرتان فوق `main`:**

```
app/src/libre/   UpdateChecker · UpdateSurface (الأجساد) · إذن التثبيت · 33 نصًّا
app/src/play/    UpdateSurface (الأسماء بلا أجساد) — ولا شيء غيره
```

`ui/update/UpdateSurface.kt` هو **الحاجز**: أربعةُ أسماءٍ تُعرَّف في الشجرتين
(`UPDATE_SECTION_IDS` · `UpdateBanner` · `UpdateAutoCheck` · `updateSection`)،
فلا سطرَ شرطيًّا في `MainActivity` ولا في `SettingsScreen`. ومن زاد اسمًا في
إحداهما زاده في الأخرى، وإلّا سقط بناءُ نكهةٍ لا يبنيها.

### الخرائط: أربعة مصادر ومحرّكان

`OfflineMaps.bind()` هو **القرار الوحيد**: يفتح الأرشيفات مرّةً، ويجسّ التغطية،
ويسلّم عين ما فتح إلى المزوّد. والمصادر:

| المصدر | من يرسم |
|---|---|
| إنترنت | osmdroid من الشبكة |
| OsmAnd | OsmAnd نفسه عبر AIDL، صورةً ساكنة |
| محلّيّة | osmdroid من أرشيف مجلّد الخرائط |
| تلقائيّ | أحسن ما يتوفّر بالترتيب |

**و‎.pmtiles‎ المتجهيّة تدخل من باب «محلّيّة» نفسه**: `PmtilesRasterArchive` يُنفّذ
`IArchiveFile` — عقدُها سطرٌ واحد، «أعطني بلاطةً بهذا الرقم» — فيقرأ المربّع المتجهيّ
ويرسمه صورةً. وبذلك دخل المحرّك الجديد في الآلة القائمة كلِّها بلا مزوّدٍ ثانٍ:
المخبأ والتقريب من مستوًى أدنى وحدُّ التكبير وقلبُ الألوان.

- **العربيّة تُرسم بـ`Canvas.drawText`** فتمرّ على مُشكِّل أندرويد وتتّصل من تلقائها.
  لا خطوطَ SDF مولَّدة ولا حاجة إليها.
- **التقريب يتجاوز مدى الأرشيف بأربع درجات** (`z14` → `z18`): النافذة تدخل في حساب
  الإحداثيّات **قبل** الرسم، فالصورة ‎256‎ بكسلًا مهما عمُق التقريب.
- **مخبأ osmdroid المشترك ممنوعٌ في المسار المحلّيّ**: بلاطات الإنترنت تُحفظ فيه،
  فتُقدَّم تحت شارة «محلّيّة» ويظنّ المستعمل أرشيفه يغطّي ما لا يغطّي.

### عقد الطبقة: الشاشة والملفّ يجب أن يتّفقا

`CameraScreen` و`VideoOverlayPainter` يرسمان **التصميم نفسه** بنسب `HudMetrics`.
فكلّ تعديلٍ في أحدهما يقتضي نظيرَه في الآخر، وإلّا كذبت الشاشة على الملفّ.

- **المواضع مطلقة لا منطقيّة** (`Arrangement.Absolute` · `AbsoluteAlignment`):
  الملفّ يُرسم بإحداثيّات بكسل، فانقلابُ اتّجاه التخطيط يقلب الشاشة وحدها.
- **الفروق المتعمَّدة اثنان لا غير:** الطبقة على الشاشة تتنحّى عن أزرار التحكّم
  (ترتفع فوق صفّها في الرأسيّ، وتنزاح عن عمودها في الأفقيّ)، والملفّ يلتصق بهامشه
  لأنّ الأزرار لا تُحرق. وثانيهما أنّ الملفّ يحصر الطبقة في **منطقةٍ آمنة**
  (`SAFE_SHORT_OF_LONG`) تنجو من اقتطاع مشغّلات الفيديو.
- **تصميم القرص يُشتقّ مرّةً:** `drawGaugeFace` نفسها ترسم الشاشة والفيديو، مجسورةً
  بـ`CanvasDrawScope`. لا تُنسخ الأوجه إلى `android.graphics.Canvas`.

### إدارة المساحة

`StorageWarden` يحذف **الأقدم فالأقدم من `Movies/GT-SPEEDOMETER` وحده**، والفيديو
دون اللقطات، وملفّاتُ الجلسة الجارية محميّةٌ بأسمائها. ولا يمسّ ملفّات المستعمل
مهما ضاق القرص.

والتصوير المتّصل لا يلُفّ من نفسه، فلا موضع للحذف بين ملفّين: عند الضيق يُنتقل إلى
مقاطع ثلاث دقائق (`EMERGENCY_SEGMENT_MINUTES`) ليصير للحذف موضعٌ دوريّ. والنبض من
`VideoRecordEvent.Status` لا من مؤقّتٍ عندنا.

## مزالق واجهتها فعلًا

- `LocationListener` في Kotlin: لا تستعمله مباشرة. أندرويد 8-10 ينادي `onStatusChanged`
  وإن لم تُنفَّذ صراحةً سقط التطبيق بـ `AbstractMethodError`. الحلّ الرسميّ
  `androidx.core.location.LocationListenerCompat`، وهي تكفينا المسألة وتقبل تحويل SAM.
- `LocationManager.FUSED_PROVIDER` (أندرويد 12+) مزوّد **إطاريّ** لا علاقة له بخدمات
  جوجل، فاستعماله لا يكسر قاعدة «لا Play Services». نستعمله احتياطًا فقط حين ينقطع
  GPS أكثر من 10 ثوانٍ، وعيّناته تُوسَم `provider=fused` في GPX.
- `Icons.Filled.TrendingUp` مهجورة — استعمل `Icons.AutoMirrored.Filled.*`.
- تفويض `animateFloatAsState` بـ `by` يحتاج `import androidx.compose.runtime.getValue`.
- مرساة الفيديو تُثبَّت في `VideoRecordEvent.Start` لا عند ضغط الزرّ؛ بينهما مئات
  الملّي ثانية.
- **`*/*` داخل تعليق KDoc يُغلق التعليق** فيتعطّل البناء برسالةٍ بعيدةٍ عن السبب.

## مزالق 0.4.0

- **ليس كلّ رمزٍ في `VideoRecordEvent.Finalize` فشلًا.** `SOURCE_INACTIVE` (4) و
  `FILE_SIZE_LIMIT` (2) و`INSUFFICIENT_STORAGE` (3) و`DURATION_LIMIT` (9) تترك
  ملفًّا سليمًا يُقرأ. `hasError()` وحده يُظهر الحفظ الناجح فشلًا.
- **CameraX يتبع دورة حياة من تربطه به.** الربط بالنشاط يعني موت التسجيل عند
  أوّل مغادرة. الجلسة تملك `LifecycleRegistry` خاصًّا، يُثبَّت عند RESUMED ما دام
  التسجيل جاريًا. وكلّ تحوّلٍ فيه على الخيط الرئيس، وإلّا سقط.
- **خدمةٌ أماميّة بنوع «كاميرا» لا تُبدأ من الخلفيّة** في أندرويد 14. تُبدأ دائمًا
  والتطبيق في المقدّمة، وتبقى ما بقيت رحلةٌ **أو** تسجيل.
- `Color.value` كلمةٌ من 64 بتًّا؛ `toInt()` عليها تعطي صفرًا (شفّافًا) لا اللون.
  للحصول على `int` لوني: `toArgb()`.
- **`LocalContentColor` لا يوفّره إلّا `Surface`/`Scaffold`.** خلفيّةٌ عبر
  `Modifier.background` لا توفّره، وافتراضه في Material 3 أسودُ صريح. تُوفَّر في
  `GtSpeedometerTheme`.
- ألوان اللوحة (`Bg`, `Accent`, …) خصائصُ `@Composable` منذ 0.4.0: لا تُقرأ داخل
  `remember {}` ولا في لامدا `AndroidView` ولا في `DrawScope`. تُرفع في جسم الدالّة
  التركيبيّة ثمّ تُمرَّر.
- `ProcessCameraProvider.getInstance` غير متزامنة: قد يغادر المستعمل قبل أن تصل،
  فيربط المستمع المتأخّر كاميرا بلا شاشة. لذلك عدّاد الأجيال في `CameraSession`.
- **PiP:** `onUserLeaveHint` لا يُنادى لإيماءة الصعود على كثيرٍ من الأجهزة، ولذلك
  `setAutoEnterEnabled` من أندرويد 12 إلى جانبه لا بدلًا منه. و`configChanges`
  يجب أن يشمل `smallestScreenSize|density` وإلّا أُعيد إنشاء النشاط عند الانكماش.

## مزالق 0.9.6 فما بعد

- **`targetRotation` في CameraX يُقرأ مرّةً عند بناء حالة الاستعمال.** والنشاط يعلن
  `configChanges` للاتّجاه فيعالجه بنفسه بلا إعادة ربط — فالصورة على الشاشة سليمة
  والملفّ مقلوب. يُبلَّغ الاتّجاه عند كلّ تهيئة، **ولا يُبلَّغ أثناء تسجيلٍ جارٍ**.
- **ولأنّ `configChanges` يشمل الاتّجاه، لا يُعاد إنشاء الشاشة عند الإدارة**: التخطيط
  يقفز في إطارٍ واحد ما لم تُحرَّك مواضعه (`ORIENTATION_MS` في `CameraScreen`).
- **الاتّجاه يُقرأ من قيود `BoxWithConstraints` لا من إعدادات الجهاز**: الشاشة قد
  تكون نصفَ نافذةٍ منقسمة أو نافذةً مصغَّرة.
- **`signingConfigs` يجب أن تسبق `productFlavors`** وإلّا ردّت `findByName` عدمًا
  بصمت. و`buildTypes.release.signingConfig` **يتقدّم** على توقيع النكهة في AGP.
- **`Intent.createChooser` يعرض كلّ من أعلن `*/*`** — محرّرَ مستنداتٍ ومساعدًا محادثًا.
  `MapApps` تفصل من أعلن نوعًا محدَّدًا، ويُقصر المنتقي عليهم بـ`EXTRA_EXCLUDE_COMPONENTS`.

## توثيقٌ يُقرأ قبل العمل على الخرائط

`docs/تجربة-الخرائط-المتجهية.md` — المصادر التي فُحصت وحكمُ كلٍّ منها، وما قِيس في
أرشيفٍ حقيقيّ (كثافة المعالم بكلّ مستوى وقيم `kind` كما وردت)، ولماذا أُزيل MapLibre،
وما يبدأ منه من عاد إلى المتجهيّ. **لا تُعد قياس ما فيه.**
